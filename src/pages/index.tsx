import React from 'react';

import { Meta } from '@/components/layouts/Meta';
import { Main } from '@/components/Main';

const Index = () => {
  return <Main meta={<Meta />} />;
};

export default Index;
